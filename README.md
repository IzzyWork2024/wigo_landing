# WiGo Landing (preservando estilos)

Este refactor **no cambia** tu HTML/CSS/JS (solo los separa):
- `index.html` (estructura)
- `css/styles.css` (contenido exacto de tus `<style>...</style>`)
- `js/main.js` (contenido exacto de tus `<script>...</script>` inline)

## Nota
Mantén tus assets en las mismas rutas que ya usabas.
